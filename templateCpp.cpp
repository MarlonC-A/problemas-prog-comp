// MarlonC-A, 2020

#include<bits/stdc++.h>
using namespace std;

#define w(x) cout<<"'"<<(#x)<<"' es "<<x<<endl;
#define EPS 1.19209e-07
#define fastIO ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
typedef long long int lol;
typedef pair<int,int> pii;
lol BIG = 1e9+7;

int64_t bigcin(){ //Método de entrada para enteros de 64 bits con signo, usar como "int64_t [var] = bigcin();"
    string cad;
    cin>>cad;
    int64_t numerote = stoll(cad);
    return numerote;
}

int main(){
    fastIO
    
return 0;
}
